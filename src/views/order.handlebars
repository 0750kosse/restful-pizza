<div class="submenu-heading__main">
  <div class="submenu-heading__main--collect">COLLECT from...</div>
  <div class="submenu-heading__main--menu">MENU</div>
  <div class="submenu-heading__main--deals">DEALS
    <i class="fas fa-angle-right"></i>
  </div>
  <div class="submenu-heading__main--basket">
    <span class="basket-total">
      <i class="fas fa-shopping-basket"></i>
      <span class="basket-amount">£{{total}}</span>
    </span>
    <span class="basket-quantity">{{amount}}</span>
  </div>
</div>
<div class="basket-container">
  {{# if items}}
  <h2 class="basket-summary__text">Your have {{totalItems}} items on your order : </h2>
  {{#each items}}
  <div class="basket-item">
    <div class="basket-item__image"></div>
    <div class="basket-item__info">
      <p class="basket-item__name"> Product: {{name}} </p>
      <p class="basket-item__price">£ Subtotal: {{subtotal}}</p>
      <p class="basket-item__quantity">Quantity: {{quantity}}</p>
    </div>
    <div class="basket-buttons">
      <button class="btn btn-primary sum" id="add" onclick="addOne()">+</button>
      <button class="btn btn-primary rest">-</button>
    </div>
  </div>
  {{/each}}
  <h2 class="basket-total__amount">Total Order: {{total}}</h2>
  <h2 class="basket-total__qty">Total Order Quantity: {{totalItems}}</h2>
  <button class="btn btn-primary rest">Checkout</button>
  {{else}}
  <h2>no products added yet</h2>
  {{/ if}}
</div>